#include "shell.h"

/**
 * full_length - Calculates the combined length of two arrays of strings.
 * @stringsA: The first array of strings.
 * @stringsB: The second array of strings.
 *
 * Return: The total length of stringsA and stringsB.
 */

int full_length(char **stringsA, char **stringsB)
{
	int countA, countB;

	for (countA = 0; stringsA[countA] != NULL; countA++)
	{

	}

	for (countB = 0; stringsB[countB] != NULL; countB++)
	{

	}
	return (countA + countB);
}
